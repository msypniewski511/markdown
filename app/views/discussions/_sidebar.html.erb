
  <h3 class="title is-5 has-text-grey-light">Topics/Channels</h3>
  <ul class="list">
    <% @channels.each do |c| %>
      <li><%= link_to c.channel, c %></li>
    <% end %>
  </ul>

  <% if has_role?(:admin) %>
    <%= link_to 'New Topic/Channel', new_channel_path, class: 'button is-info pt-1' %>
  <% end %>